const unclaimed = 'Unclaimed';
const unverified = 'Unverified';
const pendingPayment = 'Pending payment';
const verified = 'Verified';

export default { unclaimed, unverified, pendingPayment, verified };